# The Living Map — Command Post (JS-only)

Vite+React dashboard + Node.js MQTT->WS backend. See PLAN.md and docs/ona-contract.md.

## Quickstart

See docs/ona-contract.md Local dev. Simulator `backend/src/sim-publisher.js` is dev-only stand-in for ROS2 bridge.
